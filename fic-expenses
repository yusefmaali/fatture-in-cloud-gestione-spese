#!/usr/bin/env bash
# Wrapper script to run fic-expenses TUI without activating the virtual environment
# Usage: ./fic-expenses

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/venv/bin/python" -m fic_expenses.app "$@"
