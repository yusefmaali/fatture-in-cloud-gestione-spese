#!/usr/bin/env bash
# Wrapper script to run fic-expenses without activating the virtual environment
# Usage: ./fic-expenses list, ./fic-expenses create, etc.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/venv/bin/python" -m fic_expenses.cli "$@"
